# Local overrides (optional). Copy to .env.local — never commit .env.local.
# Expo merges .env.local over .env. For most workflows, use .env or .env.development instead.
# See docs/API_KEYS_AND_ENV.md and README.md.

EXPO_PUBLIC_SUPABASE_URL=https://YOUR_PROJECT_REF.supabase.co
EXPO_PUBLIC_SUPABASE_ANON_KEY=your_supabase_anon_key
